using System;

namespace MonolithEngine
{
    public static class Program
    {
        [STAThread]
        static void Main()
        {
            //using (var game = new SideScrollerGame())
            //    game.Run();
        }
    }
}
